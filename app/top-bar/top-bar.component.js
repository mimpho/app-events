'use strict';

// Register `topBar` component, along with its associated controller and template
angular.
  module('topBar').
  component('topBar', {
    templateUrl: 'top-bar/top-bar.template.html'/*,
    controller: function() {
      $(document).foundation();
    }*/
  });
